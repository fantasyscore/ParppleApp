<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Window background matches the app background (#212123).
             The default AppCompat window background is WHITE — it becomes
             visible for a frame whenever a screen mounts/detaches during
             navigation, which is the "white flash". -->
        <item name="android:windowBackground">@color/appBackground</item>
    </style>

</resources>
